# 快速开始指南

## 一、环境准备

### 1. 安装SUMO

1. 下载SUMO: https://www.eclipse.org/sumo/
2. 安装到默认路径: `C:\Program Files (x86)\Eclipse\Sumo`
3. 添加到系统PATH（或将sumo.exe复制到项目目录）

验证安装:
```bash
sumo --version
```

### 2. 安装Python依赖

```bash
pip install -r requirements.txt
```

### 3. 验证系统

```bash
python test_system.py
```

## 二、快速测试

### 方式1: 快速测试（推荐）

运行简化版测试，验证系统功能:
```bash
python quick_start.py
```

这将:
- 运行1000步仿真
- 测试模型推理
- 验证SUMO连接
- 显示基本统计信息

预期输出:
```
✅ 测试完成!
   - 总步数: 1000
   - 总奖励: XXX
```

### 方式2: 完整训练

运行完整的三阶段训练流程:
```bash
python train.py
```

这将:
- 阶段1: 世界模型预训练（~1-2小时）
- 阶段2: 安全RL训练（~2-3小时）
- 阶段3: 约束优化（~1-2小时）
- 生成XLSX结果文件

## 三、配置说明

### 基本配置文件: `configs/training_config.json`

```json
{
  "device": "cuda",           // cuda 或 cpu
  "environment": {
    "max_steps": 3600,        // 每episode最大步数
    "control_ratio": 0.25     // 控制车辆比例
  },
  "model": {
    "top_k": 5,               // 同时控制的车辆数
    "cost_limit": 0.1         // 成本约束
  }
}
```

### 快速训练配置

修改配置以加快训练:
```json
{
  "phase1": {
    "num_episodes": 10,       // 减少episode数
    "epochs": 10
  },
  "phase2": {
    "total_timesteps": 5000   // 减少训练步数
  },
  "phase3": {
    "total_timesteps": 3000
  }
}
```

## 四、常见使用场景

### 场景1: 仅评估模型

```bash
python train.py --eval-only
```

### 场景2: 生成XLSX结果

```bash
python train.py --eval-only --generate-xlsx
```

### 场景3: 分阶段训练

```bash
# 仅训练阶段1
python train.py --phase 1

# 仅训练阶段2
python train.py --phase 2

# 仅训练阶段3
python train.py --phase 3
```

### 场景4: 使用CPU训练

修改配置:
```json
{
  "device": "cpu"
}
```

## 五、查看结果

### 训练日志
```
logs/training_YYYYMMDD_HHMMSS.log
```

### 模型检查点
```
checkpoints/
├── world_model_phase1.pth
├── ppo_phase2.pth
└── final_model.pth
```

### XLSX结果文件
```
results/traffic_control_results_YYYYMMDD_HHMMSS.xlsx
```

### 训练历史
```
logs/training_history.json
```

## 六、故障排除

### 问题1: SUMO启动失败

**错误**: `TraCIException: Connection refused`

**解决方案**:
1. 检查SUMO是否安装: `sumo --version`
2. 验证配置文件路径
3. 尝试GUI模式调试:
   ```json
   {"use_gui": true}
   ```

### 问题2: CUDA内存不足

**错误**: `CUDA out of memory`

**解决方案**:
1. 使用CPU: `{"device": "cpu"}`
2. 减小batch size
3. 减少并行环境数

### 问题3: 依赖包缺失

**错误**: `ModuleNotFoundError`

**解决方案**:
```bash
pip install -r requirements.txt

# PyTorch Geometric特殊安装
pip install torch_geometric -f https://data.pyg.org/whl/torch-2.0.0+cu118.html
```

### 问题4: 训练速度慢

**优化建议**:
1. 使用GPU加速
2. 减少SUMO仿真步数
3. 降低`future_steps`参数
4. 使用更小的模型

## 七、项目结构

```
TJ_transport_v4/
├── configs/           # 配置文件
├── src/
│   ├── models/       # 神经网络模型
│   ├── env/          # SUMO环境接口
│   ├── algorithms/   # 训练算法
│   └── evaluation/   # 评估工具
├── checkpoints/      # 模型检查点
├── logs/            # 训练日志
├── results/         # 结果文件
├── train.py         # 主训练脚本
├── quick_start.py   # 快速开始
└── test_system.py   # 系统测试
```

## 八、性能基准

### 硬件配置
- CPU: Intel i7 (8核)
- GPU: NVIDIA RTX 3060 (12GB)
- RAM: 16GB

### 训练时间（参考）
- 阶段1: 1-2小时
- 阶段2: 1-2小时
- 阶段3: 30-60分钟
- **总计**: 3-5小时

### 评估指标（目标）
- OD完成率: ≥85%
- 速度标准差: ≤3.0 m/s
- 干预成本: ≤0.1

## 九、提交竞赛

1. 训练完成模型
2. 生成XLSX结果:
   ```bash
   python train.py --eval-only --generate-xlsx
   ```
3. 上传XLSX文件到竞赛平台
4. 无需提交代码

## 十、获取帮助

### 查看完整文档
```bash
cat README.md
```

### 查看训练选项
```bash
python train.py --help
```

### 运行系统测试
```bash
python test_system.py
```

---

**祝训练顺利！** 🚀
